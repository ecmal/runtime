{
  "name"    : "runtime",
  "vendor"  : "ecmal",
  "version" : "0.0.1",
  "scripts" : {
    "postinstall"   : "tsc -p src",
  	"build"         : "tsc -p src"
  },
  "bundle"   : {
    "format" : "global"
  }
}
